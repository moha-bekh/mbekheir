apiVersion: v1
kind: Service
metadata:
  name: argo-app-service
spec:
  type: NodePort
  selector:
    app: argo-app
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8888
      nodePort: 30080